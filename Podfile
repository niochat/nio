platform :ios, '13.2'

target 'Nio' do
  use_frameworks!

  pod 'SwiftMatrixSDK'
  pod 'AppCenter/Crashes'

  plugin 'cocoapods-keys', {
    :project => 'Nio',
    :keys => [
      "AppCenterAPIKey",
    ]
  }
end
